algoritmo "temperatura"
var
	r,volume,altura: real
inicio
	escreva("Entre com o raio.")
	leia(r)
	escreva("Entre com a altura.")
	leia(altura)
 	 volume <- 3.14159* (r^2) * altura
	escreva(volume)
fimalgoritmo

